# One-Page Leadership Hub

面向一般讀者的繁體中文領導學習網站，將管理文章轉成可閱讀、可反思、可留下行動紀錄的單頁學習體驗。

## 現行產品

- 公開目錄每頁四篇，最新文章優先，正式文章使用固定 ARTICLE 編號。
- 正式文章採統一模板、主題 PNG 插圖、快問快答、工具卡、自我整理與行動承諾。
- 作者工作台是獨立管理入口，只負責建立、AI 整理、保存與管理雲端草稿；正式模板預覽與發布由 Codex 完成。
- 草稿使用 `draft_<timestamp>` 作為內部 ID；只有發布 API 可以分配正式的 `article_N` ID，草稿 ID 不得出現在公開目錄或正式文章網址。
- 已發布文章的外部影音網址可由工作台修改，正式文章頁會讀取雲端最新版。
- 主圖不由工作台 AI 生成；發布後由 Codex 依專案風格製作並加入專案。
- 正式網站：https://one-page-leadership-hub.netlify.app
- 摘要播放器只出現在文章目錄卡片；正式文章內文不顯示「播放摘要」播放器，避免與閱讀內容混在一起。
- 正式目錄與文章頁採部署時產生的靜態檔案；雲端 API 僅供作者工作台草稿與發布流程使用，不在讀者每次開啟目錄時呼叫。
- ARTICLE 22「領導對話中的六種提問」已完成本機建置，包含主圖、工具卡與口播音檔。
- 工作台口播音訊的 MP3 Base64 不寫入瀏覽器文章快取，只同步至雲端，避免瀏覽器儲存空間超額。

## 邊界

- 作者工作台不是一般讀者入口，目錄與文章頁不提供工作台連結。
- 本專案與 `One-Page Personal hub` 完全分離。
- 圖片風格以 ARTICLE 15、16 與新版 17 的細緻水彩墨線、暖米色紙張、深綠與赭金色系為基準。

## 雲端動態文章流程（2026-09-10 分支方案）

- ARTICLE 01–18 保留既有靜態頁與內容，不由新流程重建或改寫。
- ARTICLE 19 起由發布 API 建立雲端正式紀錄，正式頁使用 `/article?id=article_N` 的共用標準文章模板；預覽與正式頁共用同一個渲染器。
- 新文章以既有最大正式編號 18 為基準建立下一號；編輯既有正式文章則沿用原 `article_N`，不另開重複文章。
- 沒有工具資料就不顯示空白工具卡；Markdown 表格與 inline 工具標記由渲染器轉成標準表格與工具欄。
- 音檔以獨立 `/api/audio?id=article_N` 讀取，不把大型 base64 直接塞進文章頁；主圖仍由 Codex 另行製作與放置。
- 進階 AI 生成後會驗證「課前情境快問快答」必須正好三題、每題兩個選項且各有回饋；第一次結果不完整時只重生此區塊，仍不完整則保留原草稿並阻止發布。
- 工作台不顯示快問快答與自我整理編輯欄位；正式預覽／發布流程負責依文章內容建立、檢查並套用這兩個學習活動。
- 工作台對已發布文章提供「轉為草稿」；任何保存、AI 生成或音訊處理都會先轉為草稿，避免工作台直接覆蓋正式文章。草稿可由工作台刪除並同步刪除雲端資料。
- 快問快答資料即使以雲端 JSON 字串或舊版欄位格式保存，預覽與正式頁也會先正規化後再顯示，避免題目因資料型態不同而消失。

## 發布品質門檻（2026-09-10）

- 預覽與正式發布必須呼叫同一個共用文章渲染器，不得各自維護另一套版型。
- 預覽前必須以 ARTICLE 17／18 作為基準，逐項檢查完整正文、段落標題、主圖、表格、工具卡、課前題目、自我整理、工作紀錄、行動承諾、音訊與頁尾。
- 任何選項資料都必須只呈現可讀文字，不得把 JSON、物件字串、Markdown 控制符號或內部欄位直接輸出給讀者。
- 工作工具必須包含可執行步驟與用途說明；沒有有效內容就不顯示工具卡。
- 正文過短、資料欄位不完整、主圖路徑無法載入或預覽與正式渲染結果不一致時，禁止發布。
