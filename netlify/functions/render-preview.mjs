const esc=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const md=(v)=>String(v||'尚未填寫文章正文').replace(/\r/g,'').split(/\n\s*\n/).filter(Boolean).map(b=>{const l=b.trim();if(/^##\s+/.test(l))return `<h2>${esc(l.replace(/^##\s+/,''))}</h2>`;if(/^#\s+/.test(l))return `<h1>${esc(l.replace(/^#\s+/,''))}</h1>`;return `<p>${esc(l).replace(/\n/g,'<br>')}</p>`}).join('');
export default async (request)=>{if(request.method!=='POST')return new Response('Method Not Allowed',{status:405});const a=await request.json().catch(()=>({}));const html=`<!doctype html><html lang="zh-Hant-TW"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(a.title||'未命名文章')}</title><link rel="stylesheet" href="/assets/article-learning.css"></head><body><div class="top"><a class="brand" href="/"><i>LE</i> 精萃領導™學習中心</a><span class="save">PREVIEW</span></div><div class="layout"><aside class="side"><small>ARTICLE PREVIEW</small><h2>${esc(a.title||'未命名文章')}</h2><p>${esc(a.category||'未分類')}</p></aside><main><section class="hero"><small class="kicker">${esc(a.category||'未分類')} · ${esc(a.reading_minutes||10)} MIN READ</small><h1>${esc(a.title||'未命名文章')}</h1><h2>${esc(a.subtitle||'')}</h2><p class="lead">${esc(a.summary||'')}</p></section><section><div class="head"><small class="kicker">文章正文</small><h2>閱讀內容</h2></div><article class="reading-essay">${md(a.body_markdown)}</article></section><footer class="site-footer"><div><strong>精萃領導™學習中心</strong></div></footer></main></div></body></html>`;return new Response(html,{headers:{'content-type':'text/html; charset=utf-8'}})};
