import { getStore } from '@netlify/blobs';
const e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export default async(request)=>{const id=new URL(request.url).searchParams.get('id')||'';const a=await getStore({name:'leadership-articles',consistency:'strong'}).get(id,{type:'json'});if(!a||a.status!=='published')return new Response('Not Found',{status:404});const body=String(a.body_markdown||'').split(/\n\s*\n/).filter(Boolean).map(b=>/^##\s+/.test(b.trim())?`<h2>${e(b.trim().replace(/^##\s+/,''))}</h2>`:`<p>${e(b.trim()).replace(/\n/g,'<br>')}</p>`).join('');return new Response(`<!doctype html><html lang="zh-Hant-TW"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${e(a.title)}</title><link rel="stylesheet" href="/assets/article-learning.css"></head><body><div class="top"><a class="brand" href="/"><i>LE</i> 精萃領導™學習中心</a></div><div class="layout"><aside class="side"><small>ARTICLE</small><h2>${e(a.title)}</h2><p>${e(a.category)}</p></aside><main><section class="hero"><small class="kicker">${e(a.category)} · ${e(a.reading_minutes||10)} MIN READ</small><h1>${e(a.title)}</h1><h2>${e(a.subtitle)}</h2><p class="lead">${e(a.summary)}</p></section><article class="reading-essay">${body}</article></main></div></body></html>`,{headers:{'content-type':'text/html; charset=utf-8'}})};
